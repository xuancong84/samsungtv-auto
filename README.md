This script automatically turns on Samsung TV(s) and show dashboard (opens a specific URL) during office hours on working days.

Unlike LG smart TVs, since Samsung has removed Python API for smart TVs to directly open-browser-at-URL, we can only emulate keyboard presses for entering the URL. Fortunately, pasting URL still works for now.
